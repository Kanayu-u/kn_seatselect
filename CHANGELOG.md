# Changelog

このプロジェクトの変更点を記録する。書式は [Keep a Changelog](https://keepachangelog.com/ja/1.1.0/)、
バージョンは [Semantic Versioning](https://semver.org/lang/ja/) に従う。

## [1.2.0] - 2026-09-06

実機で報告された 2 件の修正。

### Fixed
- **助手席側から運転席を選ぶと助手席で止まっていた。**
  `TaskEnterVehicle` は狙った席とは別の席で乗車を完了させることがある
  (ゲーム側が近い乗車口の席を選ぶ / アンチシャッフル系リソースが自動シャッフルを
  止めている、等)。原因を問わず直せるよう、乗車完了後に実際の座席を確認し、
  違っていれば座り直すようにした (`Config.correctSeatAfterEntry`)。
  ゲーム側の自動シャッフルと競合しないよう `Config.seatCorrectionDelayMs` だけ待ってから判定する
- **4 ドア車で左右問わず前席より後席に判定が吸われていた。**
  短押しの距離判定に**ドアのボーン**を使っていたのが原因。ドアボーンは
  ヒンジ位置にあり、後席ドアのヒンジは B ピラー = 車体のほぼ中央にあるため、
  車の横に立つと前席ドアのヒンジ (A ピラー) より必ず近くなる。
  **座席のボーン** (`seat_dside_f` 等) で測るように変更した

### Added
- `Config.correctSeatAfterEntry` / `Config.seatCorrectionDelayMs` — 乗車後の座席補正
- `Config.frontSeatBias`（既定 0.35m）— B ピラー付近で前後がほぼ等距離になるため、
  短押しの距離判定で前席をわずかに優先する。`0.0` で純粋な最短距離

## [1.1.0] - 2026-09-06

### Changed
- **座席選択 UI を簡素化し、画面の占有を減らした**
  - 画面全体の暗転 (スクリム) を廃止。クリックを拾うだけの透明な層に変更
  - 中央の大きなパネル (360px・車内を模した枠・見出し) を廃止し、
    画面下部に出る 200px 幅のリストに変更
  - 座席リストを 2 列グリッドから 1 列に変更 (奇数席の特別扱いが不要になった)
  - 操作ヒントと座席リストを同じ位置 (`bottom: 12vh`) に排他で表示。
    ヒントがそのままリストに入れ替わるので視線が動かない
  - 席数が多い車両では `max-height: 44vh` の範囲でスクロールする

## [1.0.0] - 2026-09-05

[SeatSelection by BeereMgM](https://github.com/BeereMgM/SeatSelection)（MIT）からのフォーク初版。

### Added
- 乗車キーの**長押し**（既定 300ms / `Config.holdMs`）で座席選択 UI を開く
- 座席の**占有状態**を UI に表示し、埋まっている席は選べないようにした
- 数字キー `1`〜`9` での座席選択、`ESC` / パネル外クリックで閉じる
- 車両に近づいたときの操作ヒント（NUI 描画のため日本語でも化けない）
- ロケール分離（`locales/ja.lua` / `locales/en.lua`）と、GTA 標準通知向けの ASCII フォールバック
- 施錠車両（`GetVehicleDoorLockStatus`）・大破車両の判定
- 通知経路のフォールバック（ox_lib → chat → GTA 標準通知）

### Changed
- 短押しの乗車を自前実装（一番近いドアの空席 / 運転席優先を `Config.shortPress` で選択）。
  長押しの開始でバニラの乗車が始まらないよう、車両の近くでのみ乗車 control を無効化する
- ドアまでの移動を `TaskEnterVehicle` に一本化（原案の手動経路移動を廃止）。
  原案が航空機だけ直接着席にしていた分岐も外した（2026-09-06 実機でヘリ・飛行機とも
  経路探索で乗車できることを確認）
- 対象車両の参照を netId 経由から Lua 側のエンティティ保持に変更し、
  乗車直前に距離・施錠・空席を再検証するようにした

### Fixed
- 埋まっている席を選んだときの代替席計算で UI 番号と GTA 座席インデックスを二重変換しており、
  運転席が埋まっていると存在しない座席 `-3` を指していた
- 5 席目以降（バス等）でドアのボーン名が無く、乗車処理ごと無反応になっていた
- NUI 入力中・ポーズメニュー中・乗車中・死亡中でもキーが反応していた
- `ThefeedNextPostBackgroundColor` に nil を渡していた通知処理を整理した
- リソース停止時に NUI フォーカスが残る可能性を塞いだ
